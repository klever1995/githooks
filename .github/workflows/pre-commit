#!/bin/bash

# Valida que index.html siga los estándares de HTML
if ! tidy -q -e index.html; then
    echo "ERROR: index.html tiene errores de formato. Por favor, corrige antes de hacer commit."
    exit 1
fi

# Si pasa la validación, permite el commit
exit 0
